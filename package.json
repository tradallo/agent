{
  "name": "tradallo-agent-example",
  "version": "0.1.0",
  "description": "Reference agent — queries the Tradallo Verified Record Protocol before delegating capital. Cryptographically verifies every reputation response. Fork to embed the same gate in your own agent.",
  "license": "MIT",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "tsx src/index.ts",
    "test": "node --import tsx src/policy.test.ts",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@tradallo/reputation": "^0.3.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20"
  }
}
